{
  "name": "depositsystem-backend",
  "version": "1.0.0",
  "description": "Backend services and Convex functions for LSSC ONE",
  "main": "index.js",
  "scripts": {
    "dev": "node scripts/dev.js",
    "flutter": "flutter run -d chrome",
    "convex": "npx convex dev",
    "web": "flutter run -d chrome",
    "web:release": "flutter run -d chrome --release",
    "web:build": "flutter build web",
    "listener": "ts-node services/blockchainListener.ts",
    "processor": "ts-node services/withdrawalProcessor.ts",
    "build": "tsc"
  },
  "dependencies": {
    "convex": "^1.39.1",
    "dotenv": "^16.4.5",
    "ethers": "^6.11.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "concurrently": "^9.2.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
